import React from "react";
import "./<%= component_name %>.scss";

let <%= component_name %> = React.createClass({
  render: function(){
    return(
      <div>Create your component here</div>
    )
  }
});

export default <%= component_name %>;